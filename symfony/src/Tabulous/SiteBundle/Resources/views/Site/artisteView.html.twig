{# src/OC/PlatformBundle/Resources/view/Advert/view.html.twig #}

{% extends "SiteBundle::layout.html.twig" %}

{% block title %}
  Consultation d'artiste - {{ parent() }}
{% endblock %}

{% block tabulous_body %}
<article class="box post post-excerpt">
  <header>
      <h2><a href="#">{{ artiste.nom }}</a></h2>
      <p>Song List</p>
  </header>
    <div class="info">
      <ul class="stats">
        </br>
       </br>
       </br>
       </br>
      </br>
      </ul>
    </div>
  <table class="default" cellspacing="4" cellpadding="6"  border="4" >
            <thead> 
            <tr>
             <th>Title</th>
             <th>Instrument</th>
             <th>Kind</th>
             <th>Member</th>
             <th>Rate</th>
            </tr>
           </thead>
          <tbody>
    {% for tablature in tablatures %}
       

        <tr >
            <td> <a href="{{ path('tablature_view', {'id': tablature.id}) }}">{{ tablature.nommusique }}</a></td>
            <td> {{ tablature.idinstrument.nom }}</td>
            <td> {{ tablature.idgenre.nom }}</td>
            <td><a href="{{ path('artiste_view', {'id': tablature.idmembre.id }) }}"> {{ tablature.idmembre.username }} </a></td>
            <td>{{ tablature.moyenne }}</td>
          </tr>
       
    {% else %}
      <p>Pas (encore !) de tablatures</p>
    {% endfor %}
    </tbody></table>
  
            

  <p>
    <a href="{{ path('tablature_liste') }}" class="button">Retour à la liste</a>
   
  </p>
</article>
{% endblock %}